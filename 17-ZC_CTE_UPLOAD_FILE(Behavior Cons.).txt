projection;
strict ( 2 );
use draft;
use side effects;

define behavior for ZC_CTE_UPLOAD_FILE //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use action uploadCsv;

  use association _CteItems { create; }
}

define behavior for ZC_CTE_UPLOAD_ITEMS //alias <alias_name>
{

  use update;
  use delete;

  use action processData;
  use association _CteFile;
}
